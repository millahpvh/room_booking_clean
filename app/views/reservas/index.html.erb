<p style="color: green"><%= notice %></p>

<% content_for :title, "Reservas" %>

<h1>Reservas</h1>

<div id="reservas">
  <% @reservas.each do |reserva| %>
    <%= render reserva %>
    <p>
      <%= link_to "Ver reserva", reserva %>
    </p>
  <% end %>
</div>

<%= link_to "Nova reserva", new_reserva_path %>
